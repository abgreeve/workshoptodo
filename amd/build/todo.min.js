/**
 * Render and interact with the workshop todo list.
 *
 * @module     block_workshoptodo/todo
 * @copyright  2026 Moodle
 * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
 */
define("block_workshoptodo/todo",["core/templates","block_workshoptodo/repository"],(function(Templates,Repository){const render=async function(root){const todos=await Repository.getTodos(),result=await Templates.renderForPromise("block_workshoptodo/todo",{todos:todos,rootid:root.id});Templates.replaceNodeContents(root,result.html,result.js);const form=root.querySelector('[data-region="add-todo-form"]');form.addEventListener("submit",(async function(event){event.preventDefault();const text=form.elements.text.value.trim();""!==text&&(await Repository.createTodo(text),await render(root))})),root.querySelectorAll('[data-action="toggle"]').forEach((function(checkbox){checkbox.addEventListener("change",(async function(){const id=Number(checkbox.dataset.todoId),todo=todos.find((function(currentTodo){return currentTodo.id===id}));await Repository.updateTodo({...todo,completed:checkbox.checked}),await render(root)}))})),root.querySelectorAll('[data-action="delete"]').forEach((function(button){button.addEventListener("click",(async function(){await Repository.deleteTodo(Number(button.dataset.todoId)),await render(root)}))}))};return{init:async function(selector){const root=document.querySelector(selector);window.console.log(selector),window.console.log(root),render(root)}}}));

//# sourceMappingURL=todo.min.js.map